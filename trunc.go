package main

import (
  "fmt"
  "math"
)
var appleNum int = 0 
    fmt.Printf("Number of apples?")
    num, err :=
    fmt.scan(&appleNum) // means the address of the AppleNum variable, 
    fmt.Printf(appleNum)
